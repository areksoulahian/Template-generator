export const generateMongodb = (answers) => {
  let mongoVar = '';
  let mongodbConfig = '';

  // conditionals
  // Typescript or Javascript
  // then generate /models/Users.js Schema
  // and /config/db.js
};
